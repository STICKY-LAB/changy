import Array from "../Array";
import Primitive from "../../Primitive/Primitive";

function every<T>(this : Array<T>, callback : Primitive<(value: any, index: number, array: any[]) => boolean>) {
    
}